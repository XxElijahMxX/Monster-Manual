 <!-- This displays the data from the database as cards to get all monsters -->
  <section id="monsters" class="container">
    <h1>Monsters</h1>
    

    {{!-- {{#each monsters}} --}}
    <div class="monsters">
      <h3>{{dataValues.name}}</h3>
      <p>{{dataValues.description}}</p>
    </div>
    {{!-- {{/each}} --}}
  </section>